# Thư mục node_modules — không cần push
node_modules/
client/node_modules/
server/node_modules/

# File lock (tùy chọn — giữ nếu bạn muốn đồng bộ version)
package-lock.json

# File môi trường (nếu có)
.env

# Build output
dist/
build/

# VSCode settings
.vscode/
